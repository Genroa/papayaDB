Création de la DB
	/createdb/:name

Suppression de la DB :
	/deletedb/:name
	
Requetes :
	Insertion d'un document :
		/insert/:dbname/:documentname
		
	Exporter tous les documents :
		/exportall/:dbname
		
	Select par champ :
		/request/:dbname/field/:name
	
	Select par chemin :
		/request/:dbname/path/:path/:separated/:by/:slash
		
	Options :
		Limitation min max:
			/request/:dbname/field/:name/min/:minvalue/max/:maxvalue
			
	Suppression d'un document :
		/deletedocument/:dbname/:documentname